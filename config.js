/**
*  Config variable 
*  Use this to play around with the computer's AI and the starting cards
*/
var config = {
    alpha_beta_depth:4,
    game_mode:'random_cards' // can either be random_cards or fixed_cards
}